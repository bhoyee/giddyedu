# Use an HTTPS API URL reachable by the physical device. For local testing, use the development computer's LAN address rather than localhost.
EXPO_PUBLIC_API_BASE_URL=https://api.example.giddyedu.test
